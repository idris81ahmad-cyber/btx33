# BTX3 Environment Variables
# Copy this to .env.local and fill in real values.

# Required for NextAuth (auth is used for Admin panel)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=BD4o4R2MTp5PbRAl3GPVmIdCu2Hoe1gXiLJ4bXtqOQU=

# Production admin storage (set at least one on Vercel):
# BLOB_READ_WRITE_TOKEN=   # Create a Blob store in Vercel → Storage
# GITHUB_TOKEN=            # Fine-grained PAT with contents:write on biyora-shop repo

# Site URL (for future use)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
